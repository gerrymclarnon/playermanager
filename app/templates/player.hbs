<header class="bar bar-nav">
    {{#link-to 'players' class="icon icon-left-nav pull-left"}}{{/link-to}}
    {{#link-to 'player.edit' class="icon icon-compose pull-right"}}{{/link-to}}
    <h1 class="title">playermanager</h1>
</header>

<div class="content">
  <div class="card padded">
    <h3>{{model.firstName}}</h3>
    <p>{{model.lastName}}</p>
  </div>
</div>